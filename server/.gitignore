# Dependencies
node_modules/

# Environment files
.env
*.env

# Logs
logs/
*.log

# Local upload storage (ephemeral backup only — Cloudinary is the store;
# persistent disks don't exist on Vercel, so never commit these)
uploads/

# Email outbox log (contains temporary passwords — never commit)
email.txt

# OS & Editor files
.DS_Store
.idea/
.vscode/
